You are extracting structured financial household data from a client-advisor dialog.
You MUST output ONLY valid JSON. No markdown. No explanations.

Rules:
- Use only information stated or strongly implied in the dialog.
- If a field is not mentioned, omit it (do not guess).
- Output keys must match the schema entity and field names.
- Use numbers for continuous/integer fields; use booleans for boolean fields.
- For categorical/multichoice fields: if the schema provides "allowed_values", your output MUST be one of those values exactly. If the dialog uses a synonym/phrase, map it to the closest allowed value (do not invent new values).
- If the dialog clearly mentions an entity record (e.g. a mortgage/loan/credit card, an insurance policy, a spouse), you MUST include a record for that entity even if some fields are unknown (omit only the unknown fields, not the whole record).
- Ownership rule (fields like assets.owner / income_lines.owner / protection_policies.owner): use "joint" only if the dialog explicitly says the item is joint/shared/in both names. Do NOT infer joint just from "we/our" phrasing. If the dialog explicitly says the asset is "in my name" / "titled to me" / "on my statement", prefer that specific owner even if it's described as "our" asset in plain English.
- Provider type rule (assets.provider_type): prefer the underlying institution where the asset is actually held. If the dialog says it is merely shown/viewed/grouped on an advisor platform or retirement platform, do NOT let that override an explicit underlying bank/brokerage/insurance holder.
- Income frequency rule (income_lines.frequency): this is the paycheck cadence (weekly/monthly/quarterly/annual/ad_hoc) as stated; do NOT set it to "annual" just because amount_annualized is an annual amount.
- Assets taxonomy hints (assets.asset_type + assets.subtype):
	- brokerage / taxable brokerage / investment account -> asset_type=brokerage, subtype=taxable_brokerage
	- checking / savings / bank account / cash on hand -> asset_type=cash, subtype=bank_account
	- 401k / IRA / retirement account -> asset_type=retirement, subtype=401k_ira
	- private markets / alternatives / PE/VC -> asset_type=alternatives, subtype=private_markets
	- primary residence / home -> asset_type=property, subtype=primary_residence
- Occupation group (people.occupation_group): if the dialog contains a strong cue, pick the most specific allowed value (e.g. CFO/CPA/accounting/auditor/controller/banker/analyst/portfolio -> finance; CEO/VP/founder/executive -> exec; operations/ops/logistics/project/program -> operations; sales/account executive/BD -> sales; nurse/doctor/therapist -> healthcare; teacher/professor -> education). Do NOT use retired/inactive as occupation_group; those belong to employment_status.
- For dates, use ISO format YYYY-MM-DD when explicitly stated.
- For money amounts: follow the field name (annual vs monthly).
- For numeric facts: if the dialog gives a range and later provides a more precise number for the SAME fact, use the later precise number.
- If the dialog gives only a range and no later precise number, use the midpoint of the range.
- For lists: provide an array of objects for each entity.
- Keep record counts minimal (only records supported by the dialog).

Output format requirements (VERY IMPORTANT):
- The top-level JSON MUST be an object with these keys (no wrapper keys):
	households, people, income_lines, assets, liabilities, protection_policies
- Each of those keys MUST map to an array (possibly empty).
- Do not nest the result under keys like "extracted", "result", "data", or "profile".

Completeness requirements:
- If the dialog contains ANY household-level facts (income, expenses, location, risk, etc.), output at least one object in the "households" array.
- If the input provides a household_id, include it in the appropriate id fields.

Target schema (compact JSON):
{{SCHEMA_JSON}}
